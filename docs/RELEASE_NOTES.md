更新说明
• 链式仪表「出口」显示落地节点，不再把前置分组当前选中的节点当成出口。
• 路径图在连接日志含两跳及以上时按真实跳数绘制，否则回退到已绑定的入口和落地。
• 设置新增主题页：自动/浅色/深色、主题色彩、纯黑模式。
• 链式代理页保持原有绑定能力，补充路径预览和卡片布局。
• 应用图标三个可见面改为相交的立方体，不再画成拱门。
• Telegram 发布改用 Python 上传 APK，避免频道 ID 把文件路径里的 @ 吃掉。
