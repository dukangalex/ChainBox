更新说明
• 仪表「出口」和路径图把 urltest/selector 分组展开成当前选中的具体节点，不再显示「⚡ 自动选择」这类组名。
• 路径图结合连接日志和当前策略绘制，居中显示，高度更稳，减少页面跳动。
• 工具新增「脚本」：可启用/停用多条全局覆写脚本，支持代码、URL、文件导入，以及地区分组示例。脚本为 sing-box `function main(config)`，在启动时改写运行时 JSON，不改订阅文件。
• Telegram 发布文案改用通俗说明：SHA-256 是安装包校验值，用来核对文件是否完整。
